This is a gpt-image-bridge child. Never recursively invoke gpt-image-bridge. Use native image_generation directly.
Pixel art game VFX sprite sheet for a small tactics game: chunky square pixels, hard edges, no anti-aliasing, no outline, a golden starlight color ramp (#FFFFFF, #FFF7D6, #FFE88A, #FFC940, #F29A2E) with a few pale blue sparks (#E8F0FF, #A8C8FF).
Effect: a STARLIGHT hit, 5 frames: 1 a small white flash; 2 a bright four-pointed star burst with a thin golden ring; 3 the ring widens and tiny sparkles fly out; 4 the sparkles shrink; 5 the last sparkles fading.
Layout: one horizontal row of 5 equal square cells, image size 1280x256; the impact point at the center of every cell, the burst at most 40% of the cell wide, no gaps, no borders, no labels. Transparent background (if not possible: pure black #000000).
FINAL PIXEL DELIVERY: Every game pixel is an 8x8 solid-color square aligned to one 8-pixel grid; alpha only 0 or 255, no soft alpha, no bloom, no anti-aliasing. Keep all requested frames in the exact order. Each frame stays in its own equal cell. All frames must be distinct and complete. Draw effects only, no character, no text, no grid, no border. Use a real transparent background. The final aspect ratio is intentionally very wide, so keep the requested one-row layout even if the renderer picks a different resolution. The parent will restore final exact dimensions and pixel grid. Do not use Python to draw; use native image_generation. Generate ONLY this one sheet.
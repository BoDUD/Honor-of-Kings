Pixel art game VFX sprite sheet for a small tactics game: chunky square pixels, hard edges, no anti-aliasing, no outline, a golden chi color ramp (#FFFFFF, #FFF3C4, #FFD452, #FFA526, #F06A1A).
Effect: SAFEGUARD, a golden shield on a fighter, 8 frames. In the middle of every cell there is an EMPTY person-sized space (a person about 60% of the cell height stands there, feet at 88% of the cell height) - never draw the person, and keep the inside of the shield empty and transparent. 1 golden sparks gather around the space; 2 a glowing golden oval shield outline appears around the whole space, with a brighter band of light around its middle like a monk's prayer ring; 3-6 the shield shimmers: short white highlight arcs slide along its outline, a few sparks drift up (frames 3-6 form a seamless loop); 7 the shield breaks into golden flakes; 8 the last flakes fade.
Layout FOR GENERATION: 4 columns by 2 rows, 8 frames in row-major order; image 1536x768; equal square cells; the shield about 75% of the cell height and 50% of the cell width, centered on the empty space, no gaps, no borders, no labels. Transparent background (if not possible: pure black #000000).
Render exactly 8 chronological frames in this 4x2 grid, left to right then top to bottom. Any unused final cells are completely empty transparent. No labels or gridlines. Each cell represents only 32x32 logical game pixels: extremely coarse readable shapes, hard square pixels, flat palette only, no blur, antialiasing, gradients, soft transparency or glow. Every colored pixel is fully opaque, all other pixels fully transparent. Keep empty centers where specified. Do not draw characters. All projectiles face RIGHT. This generation grid is a temporary transport layout; we will reorder entire cells into the final requested single horizontal strip without recentering or cropping the effects inside cells.
Use the native image_generation tool directly, no reference images. Never invoke the bridge recursively. Request transparent background and save the raw generated sheet as out.png.`nCRITICAL TRANSPARENCY CORRECTION: the shield is an OUTLINE surrounding EMPTY transparent space. Do not draw a horizontal line through the center. The monk prayer ring is suggested by short arcs OUTSIDE the left and right edges at middle height; it must never bridge across the empty central area. The entire central person-shaped space must be 100% transparent. In each 32x32 logical cell the outer oval is approximately 16 pixels wide and 24 high, centered (16,16), reaching x=8..24 and y=4..28. All flakes stay inside the cell. Same 4x2 generation grid, eight frames.

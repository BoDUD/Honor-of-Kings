Pixel art game VFX sprite sheet for a small tactics game: chunky square pixels, hard edges, no anti-aliasing, no outline, a golden chi color ramp (#FFFFFF, #FFF3C4, #FFD452, #FFA526, #F06A1A) plus dusty browns (#C9A27A, #8A6A4A).
Effect: KNOCK-UP impact on an enemy hit by a flying body, 6 frames: 1 a small white impact flash at the center; 2 a burst of dust and small golden sparks with a short upward swoosh of golden light; 3-4 the dust puffs outward and the swoosh rises; 5-6 the dust fades.
Layout FOR GENERATION: 3 columns by 2 rows, 6 frames in row-major order; image 1152x768; equal square cells; the impact point at the center of every cell, the effect at most 60% of the cell wide, no gaps, no borders, no labels. Transparent background (if not possible: pure black #000000).
Render exactly 6 chronological frames in this 3x2 grid, left to right then top to bottom. Any unused final cells are completely empty transparent. No labels or gridlines. Each cell represents only 32x32 logical game pixels: extremely coarse readable shapes, hard square pixels, flat palette only, no blur, antialiasing, gradients, soft transparency or glow. Every colored pixel is fully opaque, all other pixels fully transparent. Keep empty centers where specified. Do not draw characters. All projectiles face RIGHT. This generation grid is a temporary transport layout; we will reorder entire cells into the final requested single horizontal strip without recentering or cropping the effects inside cells.
Use the native image_generation tool directly, no reference images. Never invoke the bridge recursively. Request transparent background and save the raw generated sheet as out.png.
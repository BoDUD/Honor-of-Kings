Pixel art game VFX sprite sheet for a small tactics game: chunky square pixels, hard edges, no anti-aliasing, no outline, a golden chi color ramp (#FFFFFF, #FFF3C4, #FFD452, #FFA526, #F06A1A, #B83A12).
Effect: a golden DRAGON spirit rushing forward, 4 frames, seamless loop: a serpentine Chinese dragon made of golden fire flying to the RIGHT - its head at the right end of the cell with open jaws and swept-back whiskers, its body a wavy trail of golden flame thinning out to the left, embers along it; the body waves a little further every frame.
Layout FOR GENERATION: 2 columns by 2 rows, 4 frames in row-major order; image 1536x768; equal 2:1 rectangular cells; the dragon's head at the same spot near the right side of every cell, the whole dragon about 90% of the cell width, vertically centered, no gaps, no borders, no labels. Transparent background (if not possible: pure black #000000).
Render exactly 4 chronological frames in this 2x2 grid, left to right then top to bottom. Any unused final cells are completely empty transparent. No labels or gridlines. Each cell represents only 64x32 logical game pixels: extremely coarse readable shapes, hard square pixels, flat palette only, no blur, antialiasing, gradients, soft transparency or glow. Every colored pixel is fully opaque, all other pixels fully transparent. Keep empty centers where specified. Do not draw characters. All projectiles face RIGHT. This generation grid is a temporary transport layout; we will reorder entire cells into the final requested single horizontal strip without recentering or cropping the effects inside cells.
Use the native image_generation tool directly, no reference images. Never invoke the bridge recursively. Request transparent background and save the raw generated sheet as out.png.
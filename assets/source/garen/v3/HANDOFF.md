> 入库说明：两张新图已替换 `assets/source/garen/` 里的同名文件（跑步仍用第二轮的）。交付包里的 `references/`（英雄联盟模型的渲染）没有入库，用 `tools/lol/pose_ref.py` 按 PROMPTS.md 第三轮的命令重新生成。

# 盖伦正面修正版 V3

只重生成两张：garen_idle.png、garen_attack.png。每张六帧，单行，实际 RGBA 透明背景。
跑步不重画，继续使用 V2 的 garen_run.png。

每次生图同时使用两个实际图片输入：
1. 原 garen_ref.png：角色身份、造型、配色与画风。
2. 用户本轮 gpt3/ 下的同名姿势图：正面视角、每帧姿势和剑向。

本轮重点：脸和胸甲可见，尤其是待机以及普攻前三帧。新姿势参考已按用户说明处理为朝右，不再额外镜像，也不沿用 V2 背身姿势的要求。

## 交给 Claude

用本包两张同名 PNG 替换旧的待机和普攻源图。参考图放在 references/，实际生图提示词为 *.prompt.txt。
用户要求保留 GPT 原图：未用脚本切帧、重排、改像素、去背或缩放。原始画布尺寸不等于最终 6:1 方格条，切帧、脚底对齐、比例统一和像素化仍交由 Claude 完成。
姿势为参考驱动的重绘，关节、透视和动作时点仍需在游戏尺寸和播放时检查；本轮没有运行游戏或更改 PR。

| 文件 | 帧数 | 原图尺寸 |
|---|---:|---|
| `garen_idle.png` | 6 | 2172 × 724 |
| `garen_attack.png` | 6 | 2172 × 724 |

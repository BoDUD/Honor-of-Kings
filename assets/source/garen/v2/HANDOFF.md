> 入库说明：三张新图已替换 `assets/source/garen/` 里的同名文件。交付包里的 `references/`（姿势参考图是英雄联盟模型的渲染）没有入库，需要时用 `tools/lol/pose_ref.py` 按 PROMPTS.md 第二轮的命令重新生成。

# 盖伦姿势参考修正版 V2

本包提供待机、跑步、普攻三张替换源图，每张 6 帧、单行、实际 RGBA 透明背景。
角色造型参考使用上一版 garen_ref.png；动作参考使用用户提供的 garen_pose_idle.png、garen_pose_run.png、garen_pose_attack.png，生图时要求同时附上这两类图片。

修正目标：
- 待机：侧后方战备姿势，剑在身前朝右，取消扛剑待机。
- 跑步：朝右移动时向前持剑，取消剑拖在身后的动作。
- 普攻：按六帧姿势条执行转身蓄力、后拉、举剑、前劈、落剑、低位收势；最后一帧不回到扛剑。

## Claude 导入

使用本文件夹里的 garen_idle.png、garen_run.png、garen_attack.png 替换上一批的同名源图；其余素材仍用上一批。
用户要求保留 GPT 原图，本次未用脚本裁切、调色、改透明度、改像素或重排。实际画布未强制变成 6:1，仍需要按可见帧切分、统一角色比例和脚底、补边及像素化。
生成图是姿势参考驱动的重绘，不等于从游戏模型逐像素投影出的精确动画；游戏内循环、40 px 可读性和技能时点需继续验证。

| 文件 | 帧数 | 原图尺寸 |
|---|---:|---|
| `garen_idle.png` | 6 | 2172 × 724 |
| `garen_run.png` | 6 | 2172 × 724 |
| `garen_attack.png` | 6 | 2172 × 724 |

references/ 保留造型图、用户提供的真实动作参考和旧版对比图；*.prompt.txt 为实际调用提示词。
这次没有改动仓库或 PR 状态。preview.html 可在本地浏览器查看姿势参考与重绘图。
